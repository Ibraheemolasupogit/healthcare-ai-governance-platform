# Restrained foundation only — see README.md in this directory.
# No provider is configured: this scaffold declares no resources and is not
# intended to be applied against any real infrastructure.

terraform {
  required_version = ">= 1.7.0"
}
